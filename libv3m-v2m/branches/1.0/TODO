QEMU Limitations:
    - we should be able to specify the network interface we want to use for the bridge.

Makefile:
    - clean the file up.
    - generate the doxygen documentation.

General:
    - include the KVM support.

Netboot Simulation:
    - find a way to have a smaller image the current one is still way too big.

Profiles:
    - add the support of CPU specification for Xen.
