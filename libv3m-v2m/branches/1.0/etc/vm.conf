<?xml version="1.0"?>
<!DOCTYPE profile PUBLIC "" "/etc/libv3m/v3m_config.dtd">
<config>
    <qemu>
         <command>sudo qemu</command>
         <precommand>sudo modprobe tun</precommand>
    </qemu>
    <xen>
         <command>sudo xm create</command>
    </xen>
    <kvm>
        <command>sudo kvm</command>
        <precommand>sudo modprobe kvm</precommand>
    </kvm>
</config>

